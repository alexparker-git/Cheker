# [AI-AGENT] Fixed: Systematic healing applied to formatter.py
def check_status(status):
if status == True:  # INDENTATION + LOGIC error
        return "Active"
    else:
        return "Inactive"
