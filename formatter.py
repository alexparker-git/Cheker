# [AI-AGENT] Fixed: Systematic healing applied to formatter.py. Error: INDENTATION
def check_status(status):
    if status == True:  # INDENTATION + LOGIC error
        return "Active"
    else:
        return "Inactive"
